version: '3.1'
services:
  web_server:
    build:
      context: .
    environment:
      - EXTERNAL_PORT=${EXTERNAL_PORT}
      - APP_PORT=${APP_PORT}
    ports:
      - "${EXTERNAL_PORT}:${APP_PORT}"
